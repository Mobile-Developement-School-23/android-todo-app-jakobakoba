plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
    id 'kotlin-kapt'
}
apply from: "$rootDir/common.gradle"

android {
    namespace 'com.bor96dev.feature.database-api'
}
dependencies {
    implementation(
            project(':feature:todo-items-api'),
            project(':core:di'),

            deps.androidx.appcompat
    )
}
